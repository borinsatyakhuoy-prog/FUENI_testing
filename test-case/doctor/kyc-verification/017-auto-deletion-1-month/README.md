# 017 - Automatic deletion - corrections not resubmitted within 1 month, including exact reminder wording

**Result:** ⚪ NOT TESTABLE in real time (no screenshot - nothing could be captured)

## How to test
1. Get a KYC dossier into a "corrections requested" state (would itself require an admin/DSI
   reviewer to reject the submission with requested corrections).
2. Leave the corrections unresubmitted for 1 real calendar month.
3. Observe whether the account/dossier is automatically deleted, and capture the exact wording of
   any reminder notification(s) sent before that deletion.

## Expected
Unknown without the real specification/copy for this reminder and deletion behavior - this
session doesn't have access to that source document.

## Actual
**Not testable within a single session**, for two independent reasons: (1) reaching a
"corrections requested" state at all requires a real reviewer action on the admin side, which
wasn't available this session, and (2) even if it were, verifying a 1-month time-based deletion
behavior requires that much real time to pass. Same class of limitation as test case 016 - not
guessed at.

## Browser(s) tested
Not applicable - this test case was not reached.
